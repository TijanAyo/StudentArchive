const contribute = (req, res) => {
    return res.send('Contributor: Posting')
    // Posting to the studentArchive DB
    // course name
    //description - optional
    // file - .pdf, .docx e.t.c

    //@desc: Contributor would be able to make a post if authorized
}

module.exports = {
    contribute
}